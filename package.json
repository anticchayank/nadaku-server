{
  "name": "nadaku-server",
  "version": "1.0.0",
  "description": "NadaKu proxy server - yt-dlp audio stream extractor",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "python3 -m pip install -U yt-dlp --quiet || pip install -U yt-dlp --quiet"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
